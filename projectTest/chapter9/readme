PO模型，
Page Object, 页面对象模型，将每一个页面作为一个page class类，页面中所有测试元素封装成方法。
在自动测试过程中，通过页面类得到元素方法，从而对元素进行操作。
将 页面定位 和 业务操作 分离，即 测试对象 和 测试脚本 分离， 标准化 测试与页面的交互方式。
测试 搭建好的 TYNAM后台管理系统: http://localhost:8363

run.py: 执行文件，对/test/runner下组织的用例进行执行，并且生成测试报告。